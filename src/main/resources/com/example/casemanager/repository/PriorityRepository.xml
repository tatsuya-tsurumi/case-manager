<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.casemanager.repository.PriorityRepository">
	<!-- 全件検索 -->
	<select id="selectAll"
			resultType="com.example.casemanager.entity.Priority">
		SELECT priority_code, priority_name
			FROM priority
		ORDER BY priority_code
	</select>
	
	<!-- 1件検索 -->
	<select id="selectByCode"
			resultType="com.example.casemanager.entity.Priority">
		SELECT priority_code, priority_name
			FROM priority
		WHERE priority_code = #{priorityCode}
	</select>
</mapper>